function main() -> void {
    int i = 0;
    while (i < 10) {
        qubit q;
        echo("Applying Superposition");
        h(q);
        bit result = measure q;
        echo ("Measurement " + (i + 1) + " = " + result);
        i = i + 1;
    }
}