@quantum
function superposition() -> bit {
    qubit q;
    h(q);
    return measure q;
}

function main() -> void {
    int i = 0;
    int N = 10;
    int ones = 0;
    int zeros = 0;
    while (i < N) {
        bit b = superposition();
        if (b == 1b) {
            ones++;
        } else {
            zeros++;
        }
        i = i + 1;
    }
int zero_prob = zeros/N;
int one_prob = ones/N;
echo("0s " + zeros + ", " + zero_prob);
echo("1s " + ones + ", " + one_prob);
}
